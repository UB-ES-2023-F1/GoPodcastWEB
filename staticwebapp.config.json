{
    "navigationFallback": {
        "rewrite": "index.html",
        "exclude": ["*.{css,scss,js,png,gif,ico,jpg,svg}"]
    },
    "trailingSlash": "auto",
    "routes": [
        {
            "route": "/login"
        }
    ]
}
